import { HandleImageErrorDirective } from './handle-image-error.directive';

describe('HandleImageErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new HandleImageErrorDirective();
    expect(directive).toBeTruthy();
  });
});
