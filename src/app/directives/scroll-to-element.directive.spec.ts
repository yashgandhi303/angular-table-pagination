import { ScrollToElementDirective } from './scroll-to-element.directive';

describe('ScrollToElementDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollToElementDirective();
    expect(directive).toBeTruthy();
  });
});
